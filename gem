local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "🔥 Premium Script Hub 🔥",
   LoadingTitle = "⚡ Premium Hub Loading...",
   LoadingSubtitle = "by Premium Script",
   ConfigurationSaving = {
      Enabled = true,
      FolderName = "PremiumHub",
      FileName = "PremiumConfig"
   },
   Discord = {
      Enabled = false,
      Invite = "noinvitelink",
      RememberJoins = true
   },
   KeySystem = false
})

-- Services
local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Camera = Workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer
local VirtualInputManager = game:GetService("VirtualInputManager")

-- Variables
local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local HumanoidRootPart = Character:WaitForChild("HumanoidRootPart")
local Humanoid = Character:WaitForChild("Humanoid")

-- States
local States = {
   ESP = false,
   ESPBox = false,
   ESPName = false,
   ESPHealth = false,
   ESPDistance = false,
   ESPSkeleton = false,
   Fly = false,
   Noclip = false,
   Aimbot = false,
   Triggerbot = false,
   Wallhack = false,
   Mevlana = false,
   InfiniteJump = false,
   NoRecoil = false
}

-- ESP Objects
local ESPObjects = {}
local FlySpeed = 50
local AimbotFOV = 100
local MevlanaSpeed = 20
local currentPlayerIndex = 1
local MevlanaKeybind = "X"

-- Mevlana bağlantıları global tut
local MevlanaSpinConnection = nil
local MevlanaShootConnection = nil

-- Main Tab
local MainTab = Window:CreateTab("🏠 Main", 4483362458)

-- Combat Section
MainTab:CreateSection("⚔️ Combat")

-- Gelişmiş ESP Sistemi
local function CreateAdvancedESP(player)
   if player == LocalPlayer then return end
   if ESPObjects[player] then return end
   
   local character = player.Character
   if not character then return end
   
   local ESPFolder = Instance.new("Folder")
   ESPFolder.Name = "ESP_"..player.Name
   ESPFolder.Parent = character
   
   -- Highlight (Ana ESP)
   local highlight = Instance.new("Highlight")
   highlight.Name = "ESP_Highlight"
   highlight.FillColor = Color3.fromRGB(255, 0, 0)
   highlight.FillTransparency = 0.5
   highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
   highlight.OutlineTransparency = 0
   highlight.Parent = character
   
   -- BillboardGui (İsim, Sağlık, Mesafe)
   local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
   if humanoidRootPart then
      local billboardGui = Instance.new("BillboardGui")
      billboardGui.Name = "ESP_Billboard"
      billboardGui.Adornee = humanoidRootPart
      billboardGui.Size = UDim2.new(0, 200, 0, 100)
      billboardGui.StudsOffset = Vector3.new(0, 3, 0)
      billboardGui.AlwaysOnTop = true
      billboardGui.Parent = ESPFolder
      
      local frame = Instance.new("Frame")
      frame.Size = UDim2.new(1, 0, 1, 0)
      frame.BackgroundTransparency = 1
      frame.Parent = billboardGui
      
      -- İsim
      local nameLabel = Instance.new("TextLabel")
      nameLabel.Name = "NameLabel"
      nameLabel.Size = UDim2.new(1, 0, 0, 20)
      nameLabel.Position = UDim2.new(0, 0, 0, 0)
      nameLabel.BackgroundTransparency = 1
      nameLabel.Text = player.Name
      nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
      nameLabel.TextStrokeTransparency = 0
      nameLabel.TextScaled = false
      nameLabel.TextSize = 14
      nameLabel.Font = Enum.Font.SourceSansBold
      nameLabel.Parent = frame
      
      -- Sağlık
      local healthLabel = Instance.new("TextLabel")
      healthLabel.Name = "HealthLabel"
      healthLabel.Size = UDim2.new(1, 0, 0, 20)
      healthLabel.Position = UDim2.new(0, 0, 0, 20)
      healthLabel.BackgroundTransparency = 1
      healthLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
      healthLabel.TextStrokeTransparency = 0
      healthLabel.TextScaled = false
      healthLabel.TextSize = 14
      healthLabel.Font = Enum.Font.SourceSansBold
      healthLabel.Parent = frame
      
      -- Mesafe
      local distanceLabel = Instance.new("TextLabel")
      distanceLabel.Name = "DistanceLabel"
      distanceLabel.Size = UDim2.new(1, 0, 0, 20)
      distanceLabel.Position = UDim2.new(0, 0, 0, 40)
      distanceLabel.BackgroundTransparency = 1
      distanceLabel.TextColor3 = Color3.fromRGB(255, 255, 0)
      distanceLabel.TextStrokeTransparency = 0
      distanceLabel.TextScaled = false
      distanceLabel.TextSize = 14
      distanceLabel.Font = Enum.Font.SourceSansBold
      distanceLabel.Parent = frame
      
      -- Box ESP
      local boxFrame = Instance.new("Frame")
      boxFrame.Name = "ESP_Box"
      boxFrame.Size = UDim2.new(1, 0, 1, 0)
      boxFrame.BackgroundTransparency = 1
      boxFrame.BorderSizePixel = 2
      boxFrame.BorderColor3 = Color3.fromRGB(255, 0, 0)
      boxFrame.Parent = frame
      
      -- Update Loop
      local updateConnection
      updateConnection = RunService.RenderStepped:Connect(function()
         if not States.ESP or not character or not character.Parent then
            if updateConnection then updateConnection:Disconnect() end
            return
         end
         
         local targetHumanoid = character:FindFirstChild("Humanoid")
         if targetHumanoid then
            -- Sağlık güncelleme
            local health = math.floor(targetHumanoid.Health)
            local maxHealth = math.floor(targetHumanoid.MaxHealth)
            healthLabel.Text = "HP: "..health.."/"..maxHealth
            
            -- Renk değiştirme (sağlığa göre)
            local healthPercent = health / maxHealth
            if healthPercent > 0.5 then
               healthLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
            elseif healthPercent > 0.25 then
               healthLabel.TextColor3 = Color3.fromRGB(255, 255, 0)
            else
               healthLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
            end
         end
         
         -- Mesafe güncelleme
         if HumanoidRootPart and humanoidRootPart then
            local distance = (HumanoidRootPart.Position - humanoidRootPart.Position).Magnitude
            distanceLabel.Text = math.floor(distance).." studs"
         end
      end)
   end
   
   ESPObjects[player] = {Folder = ESPFolder, Highlight = highlight}
end

local function RemoveESP(player)
   if ESPObjects[player] then
      if ESPObjects[player].Highlight then
         ESPObjects[player].Highlight:Destroy()
      end
      if ESPObjects[player].Folder then
         ESPObjects[player].Folder:Destroy()
      end
      ESPObjects[player] = nil
   end
end

-- ESP Toggle
MainTab:CreateToggle({
   Name = "ESP (Gelişmiş)",
   CurrentValue = false,
   Flag = "ESP",
   Callback = function(Value)
      States.ESP = Value
      if Value then
         for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer then
               CreateAdvancedESP(player)
            end
         end
         
         Players.PlayerAdded:Connect(function(player)
            if States.ESP then
               task.wait(1)
               CreateAdvancedESP(player)
            end
         end)
         
         Players.PlayerRemoving:Connect(function(player)
            RemoveESP(player)
         end)
      else
         for _, player in pairs(Players:GetPlayers()) do
            RemoveESP(player)
         end
      end
   end,
})

-- Aimbot Function
local function GetClosestPlayer()
   local closestPlayer = nil
   local shortestDistance = AimbotFOV
   local mousePos = UserInputService:GetMouseLocation()
   
   for _, player in pairs(Players:GetPlayers()) do
      if player ~= LocalPlayer then
         local character = player.Character
         if character then
            local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
            local humanoid = character:FindFirstChild("Humanoid")
            if humanoidRootPart and humanoid and humanoid.Health > 0 then
               local screenPoint, onScreen = Camera:WorldToViewportPoint(humanoidRootPart.Position)
               if onScreen then
                  local distance = (Vector2.new(mousePos.X, mousePos.Y) - Vector2.new(screenPoint.X, screenPoint.Y)).Magnitude
                  
                  if distance < shortestDistance then
                     shortestDistance = distance
                     closestPlayer = player
                  end
               end
            end
         end
      end
   end
   
   return closestPlayer
end

MainTab:CreateToggle({
   Name = "Aimbot",
   CurrentValue = false,
   Flag = "Aimbot",
   Callback = function(Value)
      States.Aimbot = Value
      if Value then
         local connection
         connection = RunService.Heartbeat:Connect(function()
            if not States.Aimbot then
               connection:Disconnect()
               return
            end
            
            if UserInputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton2) then
               local target = GetClosestPlayer()
               if target and target.Character then
                  local targetPart = target.Character:FindFirstChild("Head")
                  if targetPart then
                     Camera.CFrame = CFrame.lookAt(Camera.CFrame.Position, targetPart.Position)
                  end
               end
            end
         end)
      end
   end,
})

MainTab:CreateSlider({
   Name = "Aimbot FOV",
   Range = {10, 500},
   Increment = 10,
   Suffix = "FOV",
   CurrentValue = 100,
   Flag = "AimbotFOV",
   Callback = function(Value)
      AimbotFOV = Value
   end,
})

-- Gelişmiş Triggerbot
MainTab:CreateToggle({
   Name = "Triggerbot (Gelişmiş)",
   CurrentValue = false,
   Flag = "Triggerbot",
   Callback = function(Value)
      States.Triggerbot = Value
      if Value then
         local connection
         connection = RunService.Heartbeat:Connect(function()
            if not States.Triggerbot then
               connection:Disconnect()
               return
            end
            
            -- Mouse ray casting kullanarak hedef tespit et
            local ray = Camera:ScreenPointToRay(UserInputService:GetMouseLocation().X, UserInputService:GetMouseLocation().Y)
            local raycastParams = RaycastParams.new()
            raycastParams.FilterDescendantsInstances = {Character}
            raycastParams.FilterType = Enum.RaycastFilterType.Blacklist
            
            local raycastResult = Workspace:Raycast(ray.Origin, ray.Direction * 1000, raycastParams)
            
            if raycastResult and raycastResult.Instance then
               local hitPart = raycastResult.Instance
               local targetCharacter = hitPart.Parent
               
               if targetCharacter:FindFirstChild("Humanoid") then
                  local targetPlayer = Players:GetPlayerFromCharacter(targetCharacter)
                  if targetPlayer and targetPlayer ~= LocalPlayer then
                     local targetHumanoid = targetCharacter:FindFirstChild("Humanoid")
                     if targetHumanoid and targetHumanoid.Health > 0 then
                        -- Team kontrolü
                        if not targetPlayer.Team or targetPlayer.Team ~= LocalPlayer.Team then
                           -- Mouse click simülasyonu
                           VirtualInputManager:SendMouseButtonEvent(0, 0, 0, true, game, 1)
                           task.wait(0.05)
                           VirtualInputManager:SendMouseButtonEvent(0, 0, 0, false, game, 1)
                        end
                     end
                  end
               end
            end
         end)
      end
   end,
})

-- Mevlana Function (Spin Bot + Auto Shoot) - Düzeltilmiş
local function StartMevlana()
   -- Önceki bağlantıları temizle
   if MevlanaSpinConnection then
      MevlanaSpinConnection:Disconnect()
      MevlanaSpinConnection = nil
   end
   if MevlanaShootConnection then
      MevlanaShootConnection:Disconnect()
      MevlanaShootConnection = nil
   end
   
   States.Mevlana = true
   
   -- Döndürme bağlantısı
   MevlanaSpinConnection = RunService.Heartbeat:Connect(function()
      if not States.Mevlana then
         if MevlanaSpinConnection then
            MevlanaSpinConnection:Disconnect()
            MevlanaSpinConnection = nil
         end
         return
      end
      
      Character = LocalPlayer.Character
      if Character and Character:FindFirstChild("HumanoidRootPart") then
         local Root = Character.HumanoidRootPart
         Root.CFrame = Root.CFrame * CFrame.Angles(0, math.rad(MevlanaSpeed), 0)
      end
   end)
   
   -- Otomatik atış bağlantısı
   MevlanaShootConnection = RunService.Heartbeat:Connect(function()
      if not States.Mevlana then
         if MevlanaShootConnection then
            MevlanaShootConnection:Disconnect()
            MevlanaShootConnection = nil
         end
         return
      end
      
      local players = Players:GetPlayers()
      local targetPlayers = {}
      
      -- Rakip takımdaki oyuncuları bul
      for _, player in pairs(players) do
         if player ~= LocalPlayer and player.Character then
            local targetHumanoid = player.Character:FindFirstChild("Humanoid")
            local targetHead = player.Character:FindFirstChild("Head")
            
            if targetHumanoid and targetHumanoid.Health > 0 and targetHead then
               -- Team kontrolü
               if not player.Team or player.Team ~= LocalPlayer.Team then
                  table.insert(targetPlayers, player)
               end
            end
         end
      end
      
      -- Sırayla hedeflere bak ve ateş et
      if #targetPlayers > 0 then
         currentPlayerIndex = currentPlayerIndex % #targetPlayers + 1
         local target = targetPlayers[currentPlayerIndex]
         
         if target and target.Character then
            local targetHead = target.Character:FindFirstChild("Head")
            if targetHead then
               -- Kamerayı hedefe çevir
               Camera.CFrame = CFrame.lookAt(Camera.CFrame.Position, targetHead.Position)
               
               -- Otomatik ateş et
               task.wait(0.05)
               VirtualInputManager:SendMouseButtonEvent(0, 0, 0, true, game, 1)
               task.wait(0.01)
               VirtualInputManager:SendMouseButtonEvent(0, 0, 0, false, game, 1)
            end
         end
      end
      
      task.wait(0.1)
   end)
   
   Rayfield:Notify({
      Title = "🌀 Mevlana",
      Content = "Mevlana aktif edildi!",
      Duration = 2,
      Image = 4483362458,
   })
end

local function StopMevlana()
   States.Mevlana = false
   
   -- Bağlantıları kapat
   if MevlanaSpinConnection then
      MevlanaSpinConnection:Disconnect()
      MevlanaSpinConnection = nil
   end
   if MevlanaShootConnection then
      MevlanaShootConnection:Disconnect()
      MevlanaShootConnection = nil
   end
   
   Rayfield:Notify({
      Title = "🌀 Mevlana",
      Content = "Mevlana devre dışı bırakıldı!",
      Duration = 2,
      Image = 4483362458,
   })
end

MainTab:CreateToggle({
   Name = "🌀 Mevlana",
   CurrentValue = false,
   Flag = "Mevlana",
   Callback = function(Value)
      if Value then
         StartMevlana()
      else
         StopMevlana()
      end
   end,
})

MainTab:CreateSlider({
   Name = "Mevlana Hızı",
   Range = {1, 50},
   Increment = 1,
   Suffix = "Hız",
   CurrentValue = 20,
   Flag = "MevlanaSpeed",
   Callback = function(Value)
      MevlanaSpeed = Value
   end,
})

-- Mevlana Keybind
MainTab:CreateKeybind({
   Name = "Mevlana Tuşu (X)",
   CurrentKeybind = "X",
   HoldToInteract = false,
   Flag = "MevlanaKeybind",
   Callback = function(Keybind)
      MevlanaKeybind = Keybind
   end,
})

-- Mevlana tuş dinleyicisi
UserInputService.InputBegan:Connect(function(input, gameProcessed)
   if gameProcessed then return end
   
   if input.KeyCode == Enum.KeyCode[MevlanaKeybind] then
      -- Toggle Mevlana durumu
      if States.Mevlana then
         StopMevlana()
      else
         StartMevlana()
      end
   end
end)

-- Movement Section
MainTab:CreateSection("🏃 Movement")

-- Fly Function
MainTab:CreateToggle({
   Name = "Fly",
   CurrentValue = false,
   Flag = "Fly",
   Callback = function(Value)
      States.Fly = Value
      if Value then
         Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
         HumanoidRootPart = Character:WaitForChild("HumanoidRootPart")
         
         local BodyVelocity = Instance.new("BodyVelocity")
         BodyVelocity.Name = "FlyVelocity"
         BodyVelocity.MaxForce = Vector3.new(9e9, 9e9, 9e9)
         BodyVelocity.Velocity = Vector3.new(0, 0, 0)
         BodyVelocity.Parent = HumanoidRootPart
         
         local BodyGyro = Instance.new("BodyGyro")
         BodyGyro.Name = "FlyGyro"
         BodyGyro.MaxTorque = Vector3.new(9e9, 9e9, 9e9)
         BodyGyro.P = 9e4
         BodyGyro.Parent = HumanoidRootPart
         
         local connection
         connection = RunService.Heartbeat:Connect(function()
            if not States.Fly then
               connection:Disconnect()
               if HumanoidRootPart:FindFirstChild("FlyVelocity") then
                  HumanoidRootPart.FlyVelocity:Destroy()
               end
               if HumanoidRootPart:FindFirstChild("FlyGyro") then
                  HumanoidRootPart.FlyGyro:Destroy()
               end
               return
            end
            
            if not HumanoidRootPart:FindFirstChild("FlyVelocity") then return end
            
            BodyGyro.CFrame = Camera.CFrame
            
            local cam = Camera.CFrame
            local moveVector = Vector3.new(0, 0, 0)
            
            if UserInputService:IsKeyDown(Enum.KeyCode.W) then
               moveVector = moveVector + cam.LookVector
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.S) then
               moveVector = moveVector - cam.LookVector
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.A) then
               moveVector = moveVector - cam.RightVector
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.D) then
               moveVector = moveVector + cam.RightVector
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
               moveVector = moveVector + Vector3.new(0, 1, 0)
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then
               moveVector = moveVector - Vector3.new(0, 1, 0)
            end
            
            BodyVelocity.Velocity = moveVector * FlySpeed
         end)
      else
         if HumanoidRootPart and HumanoidRootPart:FindFirstChild("FlyVelocity") then
            HumanoidRootPart.FlyVelocity:Destroy()
         end
         if HumanoidRootPart and HumanoidRootPart:FindFirstChild("FlyGyro") then
            HumanoidRootPart.FlyGyro:Destroy()
         end
      end
   end,
})

MainTab:CreateSlider({
   Name = "Fly Speed",
   Range = {1, 200},
   Increment = 1,
   Suffix = "Speed",
   CurrentValue = 50,
   Flag = "FlySpeed",
   Callback = function(Value)
      FlySpeed = Value
   end,
})

-- Noclip Function
MainTab:CreateToggle({
   Name = "Noclip",
   CurrentValue = false,
   Flag = "Noclip",
   Callback = function(Value)
      States.Noclip = Value
      
      local connection
      if Value then
         connection = RunService.Stepped:Connect(function()
            if States.Noclip then
               Character = LocalPlayer.Character
               if Character then
                  for _, part in pairs(Character:GetDescendants()) do
                     if part:IsA("BasePart") then
                        part.CanCollide = false
                     end
                  end
               end
            end
         end)
      end
   end,
})

-- Infinite Jump
MainTab:CreateToggle({
   Name = "Infinite Jump",
   CurrentValue = false,
   Flag = "InfiniteJump",
   Callback = function(Value)
      States.InfiniteJump = Value
   end,
})

UserInputService.JumpRequest:Connect(function()
   if States.InfiniteJump and Character and Character:FindFirstChild("Humanoid") then
      Character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
   end
end)

-- Visual Section
MainTab:CreateSection("👁️ Visual")

-- Wallhack Function
MainTab:CreateToggle({
   Name = "Wallhack",
   CurrentValue = false,
   Flag = "Wallhack",
   Callback = function(Value)
      States.Wallhack = Value
      if Value then
         for _, part in pairs(Workspace:GetDescendants()) do
            if part:IsA("BasePart") and part.Transparency < 1 and not part:IsDescendantOf(Character) then
               if not part:FindFirstChild("OriginalTransparency") then
                  local marker = Instance.new("NumberValue")
                  marker.Name = "OriginalTransparency"
                  marker.Value = part.Transparency
                  marker.Parent = part
               end
               part.Transparency = 0.7
            end
         end
      else
         for _, part in pairs(Workspace:GetDescendants()) do
            if part:IsA("BasePart") and part:FindFirstChild("OriginalTransparency") then
               part.Transparency = part.OriginalTransparency.Value
               part.OriginalTransparency:Destroy()
            end
         end
      end
   end,
})

-- FullBright
MainTab:CreateToggle({
   Name = "FullBright",
   CurrentValue = false,
   Flag = "FullBright",
   Callback = function(Value)
      if Value then
         game:GetService("Lighting").Brightness = 2
         game:GetService("Lighting").ClockTime = 14
         game:GetService("Lighting").FogEnd = 100000
         game:GetService("Lighting").GlobalShadows = false
         game:GetService("Lighting").OutdoorAmbient = Color3.fromRGB(128, 128, 128)
      else
         game:GetService("Lighting").Brightness = 1
         game:GetService("Lighting").ClockTime = 12
         game:GetService("Lighting").FogEnd = 100000
         game:GetService("Lighting").GlobalShadows = true
         game:GetService("Lighting").OutdoorAmbient = Color3.fromRGB(70, 70, 70)
      end
   end,
})

-- Character Handler
LocalPlayer.CharacterAdded:Connect(function(char)
   Character = char
   task.wait(0.1)
   HumanoidRootPart = char:WaitForChild("HumanoidRootPart")
   Humanoid = char:WaitForChild("Humanoid")
end)

-- Notification
Rayfield:Notify({
   Title = "✅ Script Loaded",
   Content = "Premium Hub başarıyla yüklendi! Tüm özellikler kusursuz çalışıyor.",
   Duration = 5,
   Image = 4483362458,
})

print("✅ Premium Script Hub Aktif!")
